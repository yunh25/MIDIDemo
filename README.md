TASK: Using CMake, create a simple JUCE standalone application which is made up of a single window with a dropdown and a button.

The dropdown is used to select a MIDI output
Button sends a random MIDI note to the MIDI output
The dropdown should be automatically populated with all of the detected MIDI outputs
